# classroom-hub-template-status=pending-confirmation
# classroom-hub-evidence=OPS-002 HEALTH-003
[Unit]
Description=INERT pending-confirmation Classroom Hub operations report timer

[Timer]
OnCalendar={{ON_CALENDAR_PENDING_CONFIRMATION}} {{TIME_ZONE_PENDING_CONFIRMATION}}
Persistent={{PERSISTENT_PENDING_CONFIRMATION}}
Unit=classroom-hub-job@operations-report.service

# Validation template only. The unresolved placeholders and .in suffix are
# intentional; do not rename, install, enable, or start this file.
